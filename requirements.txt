# Convenience install for local development and CI.
# Installs the FastAPI backend in editable mode including its dev dependencies.
# Frontend dependencies are managed separately via `cd frontend && npm install`.
-e ./backend[dev]
